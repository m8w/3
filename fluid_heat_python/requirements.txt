# Core (required)
numpy>=1.24
moderngl>=5.10
moderngl-window>=2.4
sounddevice>=0.4.6
scipy>=1.11
pyrr>=0.10

# Fast marching cubes (optional but strongly recommended - C-backed)
scikit-image>=0.22

# Archive / remote video resolution (only if you use it)
requests>=2.31
python-osc>=1.8
yt-dlp>=2024.1.1

# Nice-to-have for mesh post-processing (optional)
# trimesh>=4.0
